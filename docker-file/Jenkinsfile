pipeline {
  agent {
    docker { image nginx:latest }
  }
  stages {
    stage("Test") {
      steps{
        sh "nginx --version"
      }
    }
    stage("Test 1") {
      steps{
        sh 'docker ps -a'
      }
    }
  }
}
